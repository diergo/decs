/**
 * This package contains support for parsing and generating
 * <a href="https://en.wikipedia.org/wiki/Comma-separated_values">comma
 * separated values</a>. CSV data contains multiple rows, one per line.
 * Each row contains multiple cells separated by a character as described in
 * <a href="http://tools.ietf.org/html/rfc4180">RFC 4180</a>. The cell
 * separator and quote can be configured to be different from {@code ,} and
 * {@code "}.
 * 
 * @see diergo.csv.CsvParserBuilder
 * @see diergo.csv.CsvPrinterBuilder
 */
package diergo.csv;
